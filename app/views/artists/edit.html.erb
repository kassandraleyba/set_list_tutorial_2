<%= form_with url: "/artists/#{@artist.id}", method: :patch, loca: true do |form| %>
   <%= form.label :name, "Name:" %>
    <%= form.text_field :name %>
    <%= form.submit "Update Artist" %>
<% end %>